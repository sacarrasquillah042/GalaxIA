{
  "python.defaultInterpreterPath": "${workspaceFolder}/GalIA/bin/python",

  "// notebookFileRoot": "Fija el cwd de los notebooks a su propia carpeta. Los módulos hacen Path.cwd().parent para encontrar la raíz, así que esto no es opcional.",
  "jupyter.notebookFileRoot": "${fileDirname}",

  "python.analysis.extraPaths": ["${workspaceFolder}/src"],
  "python.terminal.activateEnvironment": true,

  "files.exclude": {
    "**/__pycache__": true,
    "**/.ipynb_checkpoints": true
  },

  "// jupyter.runStartupCommands": "Silencia el ruido de cuFFT/cuDNN al importar TensorFlow.",
  "jupyter.runStartupCommands": [
    "import os; os.environ.setdefault('TF_CPP_MIN_LOG_LEVEL', '2')"
  ]
}
